"""[미들웨어 패키지]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 미들웨어가 뭐고, 왜 필요한가? (개념)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
요청(Request)이 들어와서 응답(Response)이 나가기까지는 하나의 "통로"를 지난다.
미들웨어는 그 통로 위에 겹겹이 쌓는 "관문(layer)"이다. 모든 요청이 엔드포인트
함수에 도달하기 *전에*, 그리고 응답이 클라이언트로 나가기 *직전에* 공통 작업을
실행한다.

    [클라이언트]
        │  요청
        ▼
    ┌──────────────────────────────┐
    │ Middleware A (가장 바깥)       │  ← 요청 전처리
    │   ┌────────────────────────┐  │
    │   │ Middleware B           │  │
    │   │   ┌──────────────────┐ │  │
    │   │   │  엔드포인트 함수   │ │  │  ← 실제 비즈니스 로직 (chat.py 등)
    │   │   └──────────────────┘ │  │
    │   │ Middleware B           │  │  ← 응답 후처리
    │   └────────────────────────┘  │
    │ Middleware A                   │
    └──────────────────────────────┘
        │  응답
        ▼
    [클라이언트]

즉 "양파 껍질(onion)" 구조다. 나중에 추가된 미들웨어가 더 바깥쪽(먼저 요청을
받고, 가장 늦게 응답을 본다).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 왜 엔드포인트에 직접 안 쓰고 미들웨어로 빼는가?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
"모든 요청에 공통으로 적용"되어야 하는 횡단 관심사(cross-cutting concern)는
엔드포인트마다 복붙하면 안 된다. 대표 예:
  - 요청마다 고유 ID(trace id) 발급 → 로그 추적
  - 접근 로그 + 처리 시간 측정
  - 인증/인가 사전 검사
  - CORS, GZip 압축
  - 전역 예외 → 일관된 에러 응답 변환
  - 속도 제한(rate limit)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 어떻게 연결하는가? (HOW)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
app/main.py 의 create_app() 안에서 app.add_middleware(...) 로 등록한다.
*** 중요: 등록 순서가 곧 실행 순서다. 나중에 add 한 것이 더 바깥(=먼저 실행). ***
예) 보통 RequestContext(가장 바깥)  →  Logging  →  CORS  순으로 둔다.

FastAPI 의 미들웨어 작성 방식 2가지:
  1) BaseHTTPMiddleware 상속 (이 프로젝트가 쓰는 방식. 직관적)
  2) 순수 ASGI 미들웨어 (성능 민감/스트리밍 세밀제어 시. 더 저수준)

*** 스트리밍(SSE) 주의 ***
BaseHTTPMiddleware 는 응답 본문을 가로채는 구조라, 잘못 구현하면 SSE 스트림을
버퍼링해서 "실시간"이 깨질 수 있다. 응답 본문을 통째로 읽어 가공하는 로직은
넣지 말 것. 헤더/타이밍/로그처럼 본문을 건드리지 않는 작업만 한다.
"""
